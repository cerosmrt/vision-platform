# Copiar a .env y completar.

FLASK_ENV=development
SECRET_KEY=cambiar-esto-por-algo-largo-y-aleatorio

# Solo en producción. En dev se usa SQLite en instance/vision.db
# DATABASE_URL=postgresql://usuario:pass@host:5432/vision

# Admin inicial: se crea solo la primera vez que arranca la app.
ADMIN_EMAIL=federicomoret23@gmail.com
ADMIN_PASSWORD=cambiar-esto

# API de Claude, para diagnóstico y redacción de mails (todavía no se usa).
# ANTHROPIC_API_KEY=
